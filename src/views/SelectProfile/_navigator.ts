
import { createStackNavigator } from "@react-navigation/stack"

export const Stack = createStackNavigator()

export const NavKey = {
    ProfileOption: "Profile.MainOption",
    ProfileList: "Profile.ListScreen"
}
